# Claude Code 项目记录

## Python 环境管理

### 激活项目本地 .conda 环境

本项目使用本地的 `.conda` 环境（Python 3.11.14）。

**快捷命令（已配置在 PowerShell Profile）**：

```powershell
# 激活项目 Python 环境
ea-act

# 切换到项目目录
ea-cd
```

**完整路径方式**（如果快捷命令不可用）：
